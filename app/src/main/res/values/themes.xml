<resources>
    <!-- Base application theme -->
    <style name="DashchanMonet" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Title theme -->
    <style name="DashchanMonet.Title" parent="@style/Widget.Material3.CollapsingToolbar.Large">
        <item name="fontFamily">@font/roboto_medium</item>
    </style>

    <!-- Card theme -->
    <style name="DashchanMonet.Card" parent="Widget.Material3.CardView.Filled">
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <!-- Button theme -->
    <style name="DashchanMonet.Button" parent="@style/Widget.Material3.Button">
        <item name="fontFamily">@font/roboto_medium</item>
    </style>

</resources>